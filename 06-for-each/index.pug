doctype html
html
    head
        title Each loop in Pug
    body
        h1 For/Each Loop in Pug
        - const names = ['Dom', 'Sophie', 'Mark']
        each n, i in names
            p= n + '-' + i
        hr
        - const grades = { 'Web Dev': 85, 'Software Design': 76 }
            for n, i in grades
                p= n + '-' + i

        - const data = []
            for d in data
                p= d
            else
                strong 'No values are here!
