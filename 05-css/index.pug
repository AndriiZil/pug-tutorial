doctype html
html
    head
        title CSS in Pug
        link(rel='stylesheet' href='style.css')
        style.
            p {
                color: red;
                text-decoration: underline;
            }
    body
        - const pStyles = { 'text-align': 'center', 'text-transform': 'uppercase' };
        p(style='text-align: center; text-transform: uppercase') This is HTML File
        p(style=pStyles) This is HTML File
